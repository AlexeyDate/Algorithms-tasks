ƒаны три отсортированных (непустых) массива A, B и C.
Ќеобходимо найти такую тройку чисел A[i], B[j], C[k],
дл€ которых выражение (max(A[i], B[j], C[k]) Ч min(A[i], B[j], C[k]))
было бы минимальным из всех возможных троек. ≈сли троек с одинаковым значением несколько,
вывести ближайшую к началу массивов (приоритет A, B, C).